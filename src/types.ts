// Type Inferencing - It will assume the type

let code = "TypeScript"; // string
let num = 86; // number
let random = Math.random() > 0.5 ? 10 : '5'; // string | number


// Type Annotations - We have to explicitly specify types using annotations

let myNumber: number = 86;
let myString: string = "Hello, TypeScript!";